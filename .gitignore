# Dependencies
node_modules/

# Database
server/data/*.db
server/data/*.db-shm
server/data/*.db-wal

# Matter bridge persistence (fabric creds, session keys)
server/data/matter-storage/

# Build output
dist/
client/dist/
server/dist/

# TanStack Router — auto-generated at build time
client/src/routeTree.gen.ts

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Turbo — local build cache (binary artifacts)
.turbo/

# Docs — internal brainstorms, plans, research (keep solutions for compounded knowledge)
docs/*
!docs/solutions/
!docs/review/

# Claude Code — user-specific operational data
# (settings.json and CLAUDE.md are intentionally committable for team sharing)
.claude/settings.local.json
.claude/cache/
.claude/preferences/
screenshots/
.tmp/
